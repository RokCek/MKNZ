@model IEnumerable<web.Models.Reservation>

@{
    ViewData["Title"] = "Index";
}

<h1>Rezervacije</h1>

<p>
    <a asp-action="Create">Ustvari novo</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserNick)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EventName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserNick)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ReservationID">Urejaj</a> |
                <a asp-action="Details" asp-route-id="@item.ReservationID">Podrobnosti</a> |
                <a asp-action="Delete" asp-route-id="@item.ReservationID">Izbriši</a>
            </td>
        </tr>
}
    </tbody>
</table>
